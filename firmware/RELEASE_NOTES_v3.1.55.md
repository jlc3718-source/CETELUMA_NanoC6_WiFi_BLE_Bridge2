# Anderson Home v3.1.55

- Changes the automatic scheduler's no-enabled-event fallback from warm white to off/black.
- Keeps the existing behavior that powers the lights off when both schedule windows are inactive.
- Preserves schedules, event enable toggles, custom lights, BLE, Wi-Fi recovery, backup/restore, and OTA compatibility.
